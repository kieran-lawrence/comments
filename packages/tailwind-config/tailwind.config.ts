module.exports = {
    content: [
        '../ui/**/*.{js,ts,jsx,tsx}', // Adjust path as needed for your UI package
        // Add other paths if needed
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
