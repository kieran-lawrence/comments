# Comments API Types

This folder contains the types used within the API and also that downstream consumers may require
This will need to be moved into a private NPM repository but that costs money 😆
